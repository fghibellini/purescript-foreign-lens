{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build && pulp test"
  },
  "devDependencies": {
    "pulp": "^13.0.0",
    "purescript-psa": "^0.5.0",
    "purescript": "^0.13.0",
    "rimraf": "^2.5.4"
  },
  "dependencies": {
    "spago": "^0.10.0"
  }
}
